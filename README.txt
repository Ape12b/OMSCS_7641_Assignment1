0. <Optional> Create and activate the virtual environment
python -m venv .venv
On Windows, run:
<Path to project folder>\.venv\Scripts\Activate.ps1
Please follow instructions for installing and activating virual environment according to your OS.

1. Install dependencies
Run:
python.exe -m pip install --upgrade pip # To upgrade pip client
pip install -r requirements.txt # Install the packages provided for in requirements.txt

